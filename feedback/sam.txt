Pg. 80: I don’t think there is a one-to-many relationship in the lattice setting. Not always, anyway: Under most LWE parameters the map (s,e) -> (A,As+e) is injective. So that can’t be exactly the reason that ZK and indistinguishability are not equivalent; it seems more to do with the fact that there is no good distribution S of secrets which is invariant under contributions from the adversary (like how a uniform distribution is invariant in this way for DLOG).

Changed "does not" to "may not" to be more accurate.

X 3.1.2: says Benhamouda et al give a poly-time algorithm. But 2.7 says it's subexponential.

Page 34: "GroupGen" is not defined at this point, and is also unnecessary for just Shamir.

CK- this is true but how we use Shamir needs to compose with the rest of the thesis, so I think this is ok.

X Step 5 on page 61: uses B instead of SIM

Same step: "each coefficient is set to 0" but then most are set to be non-zero; this could be written more clearly.

CK- Most are zero, only a few are non-zero.

X Same step: i is overloaded (it's an index for both queries and players).

X Page 62: "Game 5" should be "Game 2"

X Figure 4.2: In Exp^{asec}, if b=1 and issued=0, it sets issued=1 but this does not seem to be used at any point afterwards.

Removed, this is unnecessary

X Figure 4.4: should the top dashed box (the commment) be solid?

Figure 4.8: should it be j notin corrupt in Finalize?

This is correct, we are saying players broadcast values received from corrupt parties to all other remaining honest parties.

X Page 54: "idealized idealized"

X Page 101: "pseudoradnom"
