\subsection{\DVRFOne, A Concrete Verifiable Pseudorandom Secret Sharing Scheme}
\label{dvrf-concrete}

We now define a concrete VPSS that we call \DVRFOne,
that builds upon the pseudorandom secret sharing scheme as defined by Cramer, Damg{\aa}rd, and Ishai~\cite{CramerDI05}.
However, \DVRFOne additionally defines a verify algorithm that is publicly verifiable (assuming the authenticity of the inputs),
as well as an algorithm to combine participant commitments.

As a reminder,
the pseudorandom secret sharing scheme by Cramer, Damg{\aa}rd, and Ishai~\cite{CramerDI05}  itself builds on replicated secret sharing~\cite{MitsuruSN89}.
However, Cramer et al.\ define a mechanism to non-interactively convert shares of a replicated secret sharing scheme to shares of a Shamir-secret-shared value.
They then show how this mechanism can be used as a distributed pseudorandom function;
i.e., given a replicated secret sharing of a random value,
participants can generate Shamir secret shares of an unbounded number of pseudorandom values,
without interaction.

We extend this pseudorandom secret sharing scheme by Cramer et al. and additionally define a public verifiability mechanism.
More specifically,
we define a $\verify$ function that uses the set of participants' commitments to ensure all participants performed the evaluation step  in a consistent manner.
Such consistency checks have been used in prior literature~\cite{BenhamoudaBGHIN21},
but our check is performed over commitments to secret shares,
as opposed to verifying the shares directly.
%
We prove that \DVRFOne is secure assuming a cryptographically secure hash function,
when at minimum $2t-1$ parties participate in evaluation and up to $t-1$ participants are corrupted (honest majority).

We now define \DVRFOne in more detail. We show $\keygen$ for \DVRFOne as a centralized operation,
but this operation can easily be decentralized.
\DVRFOne is additionally defined with respect to a hash function $\Hash$,
where $\Hash: \Zq \times \zo^* \rightarrow \Zq$ is a cryptographically secure hash function.

\begin{itemize}
  \item $\shinem.\setup(1^\usecp)$: Accepts as input the security parameter $\secp$,
    outputs public parameters $\pp = (\GG, q, g)$,
    where $(\GG, q, g)$ is generated by $\GroupGen(1^\lambda)$.
    $\pp$ is given as an implicit input to all other algorithms.
    %\ian{Minor nit (I've fixed): you can't say $\shinem[\Hash].\setup$ because $\setup$ outputs $q$ and the definition of $\Hash$ requires $q$ to have already been chosen. So technically, $\Hash$ can only be chosen \emph{after} $\setup$ completes.  Would it be cleaner to just have $\Hash$ be an \emph{output} of $\setup$, and so in $\pp$, and then you don't have to write it explicitly as ``$\shinem[\Hash]$'' anywhere?  Do you rely in the proofs on reprogramming or otherwise messing with the $\Hash$ that's used in $\shinem$?}
    %\chelsea{I set $\Hash$} explicitly because it is defined by the outer
    %function
  \item $\shinem[\Hash].\keygen(n, t, \minparticipants) \rightarrow \bot/(\SK_1, \ldots, \SK_n)$:
    On input the total number of participants $n$,
    the corruption threshold $t$,
    and minimum number of participants $\minparticipants$,
    the dealer performs replicated secret sharing of a random secret $\SK \in \Zq$~\cite{CramerDI05},
    following the following steps:
    \begin{enumerate}
      \item First, the dealer checks if $\minparticipants \geq 2t-1$;
        if the check fails, output $\bot$.
      \item Let $A$ be the set such that $A = {\set{n} \choose t-1}$ (notation as defined in Section~\ref{notation}),
        and let $\gamma$ be the size of $A$;
        i.e, $\gamma = \lvert A \rvert = {n \choose t-1}$.
      \item Generate $\gamma$ secret shares $\{\share_i\}_{i \in \set{\gamma}}$,
        by sampling $\share_i \rgets \Zq, i \in \set{\gamma}$.
        Implicitly,
        the secret $\SK$ is such that $\SK = \sum_{i \in \gamma} \share_i$.

      \item Initialize empty sets $\SK_1 = \emptyset, \ldots, \SK_n = \emptyset$.
      \item For each set $a_i \in A, i \in \set{\gamma}$
        and each participant identifier $j \in \set{n} \setminus a_i$,
        append $\SK_j \gets \SK_j \cup \{ (a_i, \share_i) \}$.
      \item Output $(\SK_1, \ldots, \SK_n)$.
        %\chelsea{We need to use consistent notation below with what we describe in preliminaries.}
        Each $\SK_j$ is a set of size $\delta = {n-1 \choose t-1}$,
        consisting of the tuples $(a_i, \share_i)$.
        Each $a_i$ is itself a set,
        such that $a_i \subset \set{n}$ and $\lvert a_i \rvert = t-1$,
        where if $a_i \in \sk_j$, then $j \not\in a_i$.
    \end{enumerate}

  \item $\shinem[\Hash].\generate(\partyid, \SK_\partyid, \vrfin) \rightarrow (\vrfprivout_\partyid, \vrfpubout_\partyid)$:
    On input participant identifier $\partyid \in \set{n}$,
    secret key share $\SK_\partyid$,
    and input $\vrfin \in \{0,1\}^*$,
    perform the following steps:
    \begin{enumerate}
      \item Parse $\{ (a_i, \share_i) \}_{i \in \set{\numsets}} \gets \SK_\partyid$.
      \item For each set $a_i$,
        let $\cramerpoly_{a_i}(x)$ be the degree $t-1$ polynomial defined by the set $a_i$,
        as given in Equation~\ref{eq:cramerpoly}:

        \begin{equation} \label{eq:cramerpoly}
          \cramerpoly_{a_i}(x) = \prod_{j \in a_i}\frac{j - x}{j}
        \end{equation}

        Note that $\cramerpoly_{a_i}(j) = 0$ for all $j \in a_i$ and $\cramerpoly_{a_i}(0) = 1$.

      \item Obtain the share via Equation~\ref{eq:vrf-out-eval}:
    \begin{equation} \label{eq:vrf-out-eval}
      \vrfprivout_\partyid \gets \sum_{i \in \set{\numsets}} \Hash(\share_i, \vrfin) \cdot \cramerpoly_{a_i}(\partyid)
    \end{equation}
    Note that the $\cramerpoly_{a_i}(\partyid)$ values can be precomputed by participant $k$, as they are independent of $\vrfin$, so this step requires $\numsets$ evaluations of $\Hash$ and $\numsets$ multiplications and additions in $\Zq$.

      \item Derive the commitment to $\vrfprivout_\partyid$ as $\vrfpubout_\partyid \gets g^{\vrfprivout_\partyid}$.
      \item Output $(\vrfprivout_\partyid, \vrfpubout_\partyid)$.
    \end{enumerate}

  \item $\shinem[\Hash].\verify(t, \minparticipants, \coalition, \{ \vrfpubout_j \}_{j \in \coalition}) \rightarrow \zo$:
    Perform the following steps:
    \begin{enumerate}
      \item If $\lvert \coalition \rvert \not\geq \minparticipants$,
        return $0$.
      \item Otherwise, define $\polycomm = (\polycomm_0, \polycomm_1, \ldots, \polycomm_{\lvert\coalition\rvert-1})$
        to be the tuple of $\lvert \coalition \rvert$ commitments to \emph{coefficients}
        of a polynomial $f$ defined ``in the exponent,''
        where each $\vrfpubout_i, i \in \coalition$ is a commitment to a \emph{point} on the same polynomial $f$.
      Each $\polycomm_i$ can be derived via Equation~\ref{eq:eval-vrfcomm}:
        \begin{equation} \label{eq:eval-vrfcomm}
          \polycomm_i = \prod_{j \in \coalition} \vrfpubout_j^{L_{j,i}}
        \end{equation}
        where $L_{j,i}$ is the coefficient of  the $i\supth$ term $x^i$ of the $j\supth$ Lagrange polynomial $L_j(x)$ for the coalition $\coalition$,
        as described in Section~\ref{notation}.

      \item Let $\ell = \lvert \coalition \rvert - t$.
        The verifier now checks that all participants followed the protocol honestly,
        by checking in the exponent that their shares lie on a polynomial of degree $t-1$.
        In particular,
        the verifier ensures that $\polycomm$ is a commitment to a polynomial of degree at most $t-1$,\footnote{When at least $t$ parties follow the protocol honestly, their shares will completely define this polynomial.}
        by checking the last $\ell$ entries in $\polycomm$ are equal to the identity of $\GG$:\footnote{As an optimization, note that $\polycomm_1, \ldots, \polycomm_{t-1}$ need not be computed.}
        \begin{equation} \label{eq:poly-check}
          \polycomm_{t-1 + i} = \identityG, \forall\ i \in \set{\ell}
        \end{equation}
      \item If the check in Equation~\ref{eq:poly-check} holds,
        output $1$,
        otherwise, output $0$.

    \end{enumerate}

  \item $\shinem[\Hash].\combinepub(t, \minparticipants, \coalition, \{ \vrfpubout_j \}_{j \in \coalition}) \rightarrow \vrfpubout $:
    Accepts as input the corruption threshold $t$,
    minimum participants $\minparticipants$,
    the coalition $\coalition$,
    and set of (verified) commitments $\{ \vrfpubout_j \}_{j \in \coalition})$.
    The commitments can be combined as in Equation~\ref{eq:combine-proof},
    where the coefficients $\lambda_j$ are determined by the coalition $\coalition$.
        \begin{equation} \label{eq:combine-proof}
          \vrfpubout \gets \prod_{j \in \coalition} \vrfpubout_j^{\lambda_j} = \polycomm_0
        \end{equation}
    Output $\vrfpubout$.

  \item $\shinem[\Hash].\recover(t, \minparticipants, \coalition, \{ \vrfprivout_j \}_{j \in \coalition}) \rightarrow  \bot/(\vrfprivout, \vrfpubout)$:
    Receives as input the corruption threshold $t$,
    the minimum number of participants $\minparticipants$,
    the coalition $\coalition$,
    and the set of pseudorandom secret shares $\{ \vrfprivout_j \}_{j \in \coalition}$.
    Performs the following steps:
    \begin{enumerate}
      \item If $\lvert \coalition \rvert < \minparticipants$,
        or $\coalition \not\subset \set{n}$,
        output $\bot$.
      \item For each $\vrfprivout_j, j \in \coalition$,
        derive $\vrfpubout_j \gets g^{\vrfpubout_j}$.
      \item If $\DVRFOnem.\verify(t, \minparticipants, \coalition, \{ \vrfpubout_j\}_{j \in \coalition}) \neq 1$,
        then output $\bot$.
      \item Otherwise, the shares can be combined as in Equation~\ref{eq:combine-out},
    where the $\lambda_j$ are determined by the coalition $\coalition$.
        \begin{equation} \label{eq:combine-out}
          \vrfprivout \gets \sum_{j \in \coalition} \vrfprivout_j \cdot \lambda_j
        \end{equation}
      \item Derive the commitment to $\vrfprivout$ as $\vrfpubout \gets g^{\vrfprivout}$.
      \item Output $(\vrfprivout, \vrfpubout)$.
    \end{enumerate}

\end{itemize}

\paragraph{Correctness.}
By correctness of pseudorandom secret sharing as defined by Cramer et al.~\cite{CramerDI05},
when key generation is honestly performed,
the $\generate$ algorithm produces secret shares $\vrfprivout_i = f(i)$
that are points on a degree $t-1$ Shamir secret sharing polynomial $f(x) = \sum_{i \in \set{\gamma}} \Hash(\share_i,\vrfin) \cramerpoly_{a_i}(x)$.
The combined commitment $\vrfprivout = \sum_{i \in \coalition}
\vrfprivout_i \lambda_i$ for $\coalition \subseteq \set{n}, \lvert \coalition
\rvert \geq 2t-1$ is a commitment to a pseudorandom secret value
$\vrfprivout = f(0) = \sum_{i \in \set{\gamma}} \Hash(\share_i,\vrfin)$.
$\combinepub$ and $\recover$ simply perform polynomial interpolation of these values with respect to the set of participants,
and so are likewise correct.

$\verify$ is correct because honest parties output commitments $\vrfpubout_i  = g^{f(i)}$ for the degree $t-1$ polynomial $f$ defined above.
Therefore, if all parties are honest, the coefficients of $x^t$, \dots, $x^{\lvert\coalition\rvert-1}$ of $f(x)$ will be 0, so the commitments $\polycomm_t$, \dots, $\polycomm_{\lvert\coalition\rvert-1}$ to those coefficients will be $\identityG$
(the identity element of $\GG$), and so $\verify$ will output 1.
% CK- for correctness, we assume all parties follow the protocol honestly.
%When parties follow the protocol honestly,
%the polynomial interpolating $(\vrfpubout_j$ values in the exponent will be exactly $f(x)$ if its degree is at most $t-1$.
%(and so all parties output the correct values) and will have degree at least $t$ if any corrupt party output an incorrect value.

\subsubsection{Security}
\label{shine-security-thm}

\DVRFOne is verifiable, unique, and pseudorandom.
We give the corresponding proofs next.

\begin{theorem} \label{thm:dcvrf-verifiable}
\DVRFOne is information-theoretically verifiable,
assuming the adversary controls at most $t-1$ participants,
at least $t$ participants are honest, and
participants exchange messages over an authenticated channel.
\end{theorem}
% \ian{The constraint on {n-1 \choose t-1} = poly(n) doesn't enter into
% Theorems 1 and 2, because they're information theoretic (and never run
% Gen anyway).}


\begin{proof}
  Recall that in the verifiability game,
  the adversary is required to provide an input $\vrfin$,
  a coalition $\coalition \subseteq \set{n}$,
  and a set $ (\vrfpubout_i)_{i \in \coalition \cap \corrupt}$,
  such that $ (\vrfpubout_i)_{i \in \coalition \cap \corrupt}$ does not equal the set $(\vrfpubout_i')_{i \in \coalition \cap \corrupt}$,
  where $(\vrfprivout_i', \vrfpubout_i') \gets \DVRFOnem.\generate(i, \sk_i, \vrfin)$ is obtained honestly.
  The environment derives the values
  $(\vrfprivout_j, \vrfpubout_j) \gets \DVRFOnem.\generate(j, \sk_j, \vrfin), j \in \coalition \cap \honest$
  for honest participants, reflecting the requirement that participants' messages are received over authenticated channels.
%
  The adversary wins when
  $\DVRFOnem.\verify(t, \minparticipants, \coalition, S)$ outputs $1$, where
  $S = (\vrfpubout_j)_{j \in \coalition}$.

  For $\DVRFOnem.\verify(t, \minparticipants, \coalition, S)$ to output $1$,
  then Equation~\ref{eq:poly-check} must hold.
  But for Equation~\ref{eq:poly-check} to hold,
  then $(\vrfpubout_j)_{j \in \coalition}$ must define a degree $t-1$ polynomial in the exponent.
  However, this polynomial is information-theoretically defined by the set of honest participant commitments  $(\vrfpubout_j)_{j \in \coalition \cap \honest}$,
  because we assume $\lvert \honest \rvert \geq t$,
  and a degree $t-1$ polynomial is uniquely defined by $t$ points.

  Hence, when corrupted participants do not honestly follow the protocol,
  then the resulting polynomial committed to in $S$ will be of higher degree than $t-1$.
  As such, \DVRFOne is information-theoretically verifiable.
\end{proof}

\begin{theorem} \label{thm:dcvrf-unique}
\DVRFOne is information-theoretically unique,
assuming the adversary controls at most $t-1$ participants,
at least $t$ participants are honest, and
participants exchange messages over an authenticated channel.
\end{theorem}


\begin{proof}
  In the uniqueness game given in Figure~\ref{fig:dcvrf-unique},
  the adversary is required to provide an input $\vrfin$ and the tuples
  $(\coalition, (\vrfpubout_i)_{i \in \coalition \cap \corrupt}), (\coalition', (\vrfpubout_i')_{i \in \coalition' \cap \corrupt})$.
  The environment then derives honest parties' contributions
  $( \vrfpubout_j) \gets \DVRFOnem.\generate(j, \sk_j, \vrfin)$, again reflecting the authenticated channel,
  and the environment derives the commitment sets
  $S_1 = (\vrfpubout_j)_{j \in \coalition \cap \honest} \cup (\vrfpubout_i)_{i \in \coalition \cap \corrupt}$
  and $S_2 = (\vrfpubout_j)_{j \in \coalition' \cap \honest} \cup (\vrfpubout_i')_{i \in \coalition' \cap \corrupt}$.
  The adversary wins if both sets are valid for their respective coalitions,
  and yet the output of $\DVRFOnem.\combinepub$ outputs different combined commitments.

  However,
  for $\DVRFOnem.\verify(\coalition, S_1)$ and $\DVRFOnem.\verify(\coalition', S_2)$ to both output $1$,
  then Equation~\ref{eq:poly-check} must hold.
  But for Equation~\ref{eq:poly-check} to hold,
  then $S_1$ and $S_2$ must each define a degree $t-1$ polynomial.
  Further, because we assume at least $t$ honest participants,
  then $S_1$ and $S_2$ must agree on at minimum $t$ points.

  %Hence, when corrupted participants do not honestly follow the protocol,
  %then information-theoretically,
  %either $S_1$ or $S_2$ will commit to a polynomial of higher degree than $t$,
  %and so  Equation~\ref{eq:poly-check} cannot hold for both sets.

  As such, when both $\DVRFOnem.\verify(\coalition, S_1)$ and $\DVRFOnem.\verify(\coalition, S_2)$ both output $1$,
  then the sets $S_1$ and $S_2$ must define the same degree $t-1$ polynomial.
  Because $\DVRFOnem.\combinepub$ simply performs polynomial interpolation in the exponent to output the commitment to the constant term of the polynomial,
  then information-theoretically,
  it must be the case that $\DVRFOnem.\combinepub(S_1) = \DVRFOnem.\combinepub(S_2)$.
  Therefore, uniqueness likewise holds information-theoretically for \DVRFOne.
\end{proof}

\begin{figure}[t]
  \centering
	%\begin{linedgamespec}
	\begin{pchstack}[boxed]
    \procedure{ $\SIMEval(\vrfin, \vrfprivout, \corrupt, n, \{ \sk_j\}_{j \in \corrupt})$ }{
      \vrfpubout \gets g^{\vrfprivout} \\
      (\vrfprivout_j, \vrfpubout_j) \gets \DVRFOnem.\generate(j, \sk_j, \vrfin), \text{ for } j \in \corrupt \\
      \text{\algcom This step is possible because \DVRFOne is verifiable,} \\
      \text{\algcom and so it is guaranteed that the correct $(\vrfprivout_j, \vrfpubout_j), j \in \corrupt$ are derived } \\
      \text{Define } f(x) = \vrfprivout \cdot L_0(x) + \sum_{j \in \corrupt} \vrfprivout_j \cdot L_j(x) \\
      \text{\algcom $L_i(x)$ is the Lagrange polynomial defined by $\corrupt \cup \{ 0\}$} \\
      \honest = \set{n} \setminus \corrupt \\
      \pcfor i \in \honest \pcdo \\
      \pcind \text{Derive } \vrfprivout_i \gets f(i) \\
      \pcind \vrfpubout_i \gets g^{\vrfprivout_i} \\
      \pcreturn (\vrfpubout, \{ (\vrfprivout_i, \vrfpubout_i)\}_{i \in \honest})
		}
	\end{pchstack}
	%\end{linedgamespec}
\caption{Simulating algorithm $\SIMEval$ for \DVRFOne.}
\label{fig:simeval-shine}
\end{figure}
%

\begin{theorem} \label{thm:dcvrf-pseudo}
\DVRFOne is pseudorandom in the Random Oracle Model,
assuming the adversary controls at most $t-1$ participants,
at least $t$ participants are honest,
\DVRFOne is verifiable,
and where $(n, t) \in \mathbb{N}$  are such that ${n-1\choose t-1} = \text{poly}(n)$.
%
Concretely,
  \begin{equation} \label{eq:thm-pseudo-shine}
  \advant{psdr}{\DVRFOnem,\adv}(\secp, n, t, \minparticipants) \leq \hgenerictotal/q
\end{equation}
where $\hgenerictotal$ is the number of times $\adv$ is allowed to query $\Hash$.
\end{theorem}

\begin{proof}
  We prove Theorem~\ref{thm:dcvrf-pseudo} by a sequence of games.

  \paragraph{\underline{Game~0.}}
  This is the peudorandomness game as defined in Figure~\ref{fig:dcvrf-pseudo} instantiated with \DVRFOne,
  when $b=0$.
  Let $W_0$ be the event that $\adv$ outputs $b'=1$ in Game~0.

%\ian{You were mixing up the two styles of game hopping.  Here, you're using the ``Game 0 is $b=0$, ..., Game~$k$ is $b=1$'' style.  In that style, $W_i$ is the event that $\adv$ outputs 1 in Game~$i$, and the adversary's advantage is $\frac12 |\Pr[W_k]-\Pr[W_0]|$.  It can't be the event that $\adv$ \emph{wins} Game~$i$ (as you had it before), since the adversary can trivially win Game~0 by always outputting $b'=0$. This also meant the last game wasn't quite right.  (Also, you missed a step; your old Game~2 wasn't the $b=1$ case as you'd claimed. It was the $b=1$ case with also the environment aborting if $\Hash$ was queried on the magic $\share$ value. So you need to undo that as well. I've done all that.)}

  \paragraph{\underline{Game~1.}}
  Let $\sk_\corrupt = \cup_{i \in \corrupt} \sk_i$,
  and let $\sk_\honest = \cup_{j \in \honest} \sk_j$.
  Then $\sk_\honest \setminus \sk_\corrupt$ is necessarily non-empty, so let
  $\targetshinekey$ be such that $(\cdot, \targetshinekey) \in \sk_\honest \setminus \sk_\corrupt$.

  Note that this $\targetshinekey$ will be random in $\Zq$ and unknown to $\adv$.
  Then the only difference between Game~0 and Game~1 is that
  if $\adv$ queries $\Hash$ on any input $(\targetshinekey, \cdot)$,
  the environment aborts.


  \paragraph{Difference between Game~1 and Game~0.}
  When $\adv$ is allowed to make up to $\hgenerictotal$ queries to $\Hash$,
  then the probability the environment aborts is at most $\hgenerictotal/q$.
  Let $W_1$ be the event that $\adv$ outputs $b'=1$ in Game~1.
  Then

  \begin{equation} \label{eq:dcvrf-game1}
    \lvert \Pr[W_1] - \Pr[W_0] \rvert \leq \frac{\hgenerictotal}{q}
  \end{equation}

  \paragraph{\underline{Game~2.}}
  This is the pseudorandomness game as in Figure~\ref{fig:dcvrf-pseudo},
  when $b=1$,
  and where $\SIMEval$ is as shown in Figure~\ref{fig:simeval-shine},
  along with the abort condition of Game~1.

  On start,
  the environment initializes a table $Q \gets \emptyset$.
  Then, when $\adv$ queries $\oracle^{\generate}$ on input $(\partyid, \vrfin)$,
  instead of performing $\DVRFOnem.\generate$ honestly,
  the environment first checks if $Q[\vrfin] \neq \bot$;
  if the check holds,
  it parses
  $(\vrfpubout, \{ (\vrfprivout_i, \vrfpubout_i)\}_{i \in \honest} ) \gets Q[\vrfin]$ and
  returns $(\vrfprivout_\partyid, \vrfpubout_\partyid)$.

  Otherwise, the environment samples $\vrfprivout \rgets \vrfprivdomain$,
  and then obtains
  $(\vrfpubout, \{ (\vrfprivout_i, \vrfpubout_i)\}_{i \in \honest} ) \gets \SIMEval(\vrfin, \vrfprivout, \corrupt, n, \allowbreak \{ \sk_j\}_{j \in \corrupt})$,
  where $\SIMEval$ is defined as in Figure~\ref{fig:simeval-shine}.
  Note that $\SIMEval$ requires that \DVRFOne to be verifiable,
  and hence that $\lvert \honest \rvert \geq t$.
  The environment updates $Q[\vrfin] = (\vrfpubout, \{ (\vrfprivout_i, \vrfpubout_i)\}_{i \in \honest})$,
  and then returns $(\vrfprivout_\partyid, \vrfpubout_\partyid)$.

  \medskip

  \noindent \textbf{Analysis of $\SIMEval$}
  Because Game~$1$ aborts on the condition that the adversary guesses the honest participants' secret key,
  in Game~2,
  the adversary must win with knowledge of only $t-1$ secret keys.
  However, because we assume that the number of honest parties is at least $t$,
  then the honest parties will completely determine the combined $\vrfprivout$.

  Furthermore,
  because $\DVRFOnem$ is verifiable,
  then $\SIMEval$ can derive the corrupt parties' contributions $(\vrfprivout_j, \vrfpubout_j), j \in \corrupt$ before it determines honest parties' contributions.
  As such,  corrupt parties cannot adaptively choose their contributions after
  the fact to influence the resulting
  $\vrfpubout \gets  \DVRFOnem.\combine(t, \minparticipants, \{ \vrfpubout_i\}_{i \in \coalition^*})$.
  Hence, the simulation by $\SIMEval$ is perfect.

  \paragraph{Difference between Game~2 and Game~1.}
  In Game~1, $\oracle^{\generate}(\partyid, \coalition, \vrfin)$ returns
  $(f(k), g^{f(k)})$ where $f(x) = \sum_{i \in \set{\delta}} \Hash(\share_i, \vrfin) \cdot L'_{a_i}(x)$ defined as in Equation~\ref{eq:vrf-out-eval}.
  Since $\Hash(\targetshinekey, \vrfin)$ is unknown to $\adv$,
  $f(x)$ is a random degree-$(t-1)$ polynomial that passes through $(j,y_j)_{j \in \corrupt}$.

  In Game~2, $\oracle^{\generate}(\partyid, \coalition, \vrfin)$ returns $(f(k), g^{f(k)})$,
  where $f(x) =  \vrfprivout \cdot L_0(x) + \sum_{j \in \corrupt} \vrfprivout_j \cdot L_j(x)$.
  Since $\vrfprivout$ is random,
  this is also a random degree-$(t-1)$ polynomial that passes through $(j,y_j)_{j \in \corrupt}$.

  There is no difference between the distributions of these outputs.
  Let $W_2$ be the event that $\adv$ outputs $b'=1$ in Game~2. Then

  \begin{equation} \label{eq:dcvrf-game2}
    \lvert \Pr[W_2] - \Pr[W_1] \rvert = 0
  \end{equation}

  %\ian{What is this new game?  Game 2 is \emph{already} the pseudorandomness game with $b=1$, plus the abort condition.  (It used to say that explicitly, but you took it out?)  So what does ``except that $b$ is set to $b=1$'' mean, if it's already like that in Game 2?  I don't see what this new game hop is doing?}
  %\chelsea{It used to say instantiated with shine, which is wrong. I clarified above.}


  \paragraph{\underline{Game~3.}}
  Game~3 is
  identical to Figure~\ref{fig:dcvrf-pseudo} instantiated with \DVRFOne,
  when $b=1$.


  \paragraph{Difference between Game~3 and Game~2.}
  The only difference between Game~3 and Game~2 is that Game~2 has the abort condition, but Game~3 does not.
  Let $W_3$ be the event that $\adv$ outputs $b'=1$ in Game~3.
  Then as above,

  \begin{equation} \label{eq:dcvrf-game3}
    \lvert \Pr[W_3] - \Pr[W_2] \rvert \leq \frac{\hgenerictotal}{q}
  \end{equation}

  \paragraph{\underline{Finishing the Proof.}}
  Combining Equations~\ref{eq:dcvrf-game1}--\ref{eq:dcvrf-game3} yields

  \begin{align*}
  \begin{split}
  \advant{psdr}{\DVRFOnem,\adv}(\secp, n, t, \minparticipants)
  & = \bigabs{\Pr[ \PseudoExp_{\DVRFOnem,\adv}(\secp, n, t, \minparticipants) = 1] - 1/2} \\
    & = \bigabs{\Pr[ b'=0 \wedge b=0 ] + \Pr[ b'=1 \wedge b=1 ] - 1/2} \\
  & = \left|\frac{1}{2}(1-\Pr[W_0]) + \frac{1}{2}\Pr[W_3] - \frac{1}{2}\right| \\
  & = \left|\frac{1}{2}(\Pr[W_3]- \Pr[W_0])\right| \le \frac{\hgenerictotal}{q},
  \end{split}
  \end{align*}
  which gives Equation~\ref{eq:thm-pseudo-shine}.
  This completes the proof.
\end{proof}


