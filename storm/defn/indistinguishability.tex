\subsection{Indistinguishability}
\label{defn:indistinguishability}

The security of any key generation protocol requires that the secret key be hard to guess or learn from the public transcript.
In the single-party setting, the adversary does not have any influence over the sampling of key material,
and so guaranteeing this property is straightforward.
However, because the adversary is an active participant in a distributed key generation protocol,
we must ensure that a subset of at most $t-1$ colluding players cannot bias key material.

To establish this property,
we require that a DKG must generate keys that are \emph{indistinguishable} from keys output by the target key generation algorithm.
This property is implied by the notion of secrecy if the underlying
construction assumes a one-to-one correspondence between secret and public
keys.
For example, the Gennaro et al.\ notion of simulatability assume that indistinguishability of the resulting keypair is an implicit consequence of the ability to simulate with respect to a public key.
However, simulatability does not necessarily imply indistinguishability,
because the zero-knowledge game \emph{only} guarantees the indistinguisahbility of public key material between the real and simulated environments.
If there is a one-to-one relation between public and secret keys,
proving zero-knowledge implies indistinguishability.
For cryptosystems where a one-to-many relation exists between public and private keys,
such as in lattice-based cryptosystems,
proving that a DKG is zero-knowledge does \emph{not} imply indistinguishability from its target key generation scheme.


Because our generic construction similarly assumes a one-to-one correspondence
of secret and public keys, we do not make use of this notion of
indistinguishability in the proof of security for our generic construction, as
the proof of zero-knowledge implies indistinguishability.
Hence, for future reference, we give the notion of indistinguishability more formally next.

The advantage of a distinguishing adversary $\distinguisher$ against a DKG $\dkg$ in the indistinguishability experiment in Figure~\ref{fg:indistinguishability} with respect to the target key generation algorithm $\targetkeygen$ and participating adversary $\adv$ is
\[
  \advantind{\dkg,\targetkeygen,\adv,\distinguisher}(\lambda) = \bigabs{ \Pr[\IndistinguishabilityExp_{\dkg,\targetkeygen,\adv,\distinguisher}(\lambda) = 1] - 1/2  }
\]

\begin{definition}
A DKG $\dkg$ is \defn{indistinguishable} from its target key generation algorithm if
for all probabilistic polynomial-time adversaries $\adv$ and computationally unbounded distinguishers $\distinguisher$,
$\advantind{\dkg,\targetkeygen,\adv,\distinguisher}(\lambda)$  is negligible.
\end{definition}

The attack game in Figure~\ref{fg:indistinguishability} allows for two distinct adversaries, $\adv$ and $\distinguisher$.
The oracles $\oraclekg$ and $\RO$ are defined in Figure~\ref{fg:keygen-o}.
$\adv$ participates in the protocol and controls up to $t-1$ players.
$\distinguisher$ is simply given two sets of keypairs after the protocol completes,
and is required to guess which keypair was generated by the distributed key generation protocol and which by the target key generation protocol.
Note that $\adv$ and $\distinguisher$ are assumed to be black box and act arbitrarily,
but are not allowed to share state.
Otherwise, the game could trivially be won simply by $\adv$
informing $\distinguisher$ which public key was output from the distributed key generation protocol.
To similarly prevent this trivial win condition,
the keypair output from the distributed key generation execution must be valid,
otherwise $\adv$ could force the protocol to output an invalid keypair,
%\ian{Wait: isn't the DKG totally broken if the adversary could force the protocol to output an invalid keypair?}
% Yes, but we need to encode that requirement somewhere in our security games.
which $\distinguisher$ could trivially distinguish from the output of $\targetkeygen.\keygen$.

\begin{figure}[!hbt]

  \begin{pchstack}[boxed,center,space=1em]
  \begin{pcvstack}
    \procedure[linenumbering]{$\IndistinguishabilityExp_{\dkg,\targetkeygen,\adv,\distinguisher}(\lambda)$}{
    b \rgets \zo \\
    \randcoin \rgets \zo \text{\algcom Random coin for losing conditions} \\
    \roundcounter \gets 0 \text{\algcom Ensure rounds are queried in order}\\
    \invecp_{11} \gets \emptyset, \ldots, \invecp_{1n} \gets \emptyset \text{\algcom Simulate peer-to-peer channels} \\
    (n, t, \corrupt, \advstate) \gets \adv(\lambda) \\
    \pcif n < t \vee \corrupt \not\subset \set{n} \vee \lvert \corrupt \rvert \geq t
    \pcthen \randcoin \rgets \zo;\ \pcreturn \randcoin \\
    \honest \gets \set{n} \setminus \corrupt \\
    \pcfor i \in \honest \pcdo \\
    \pcind (\party_i, \outvecp_{0i}, \bmsg_i) \gets \dkgkeygen_0[\Hash](\lambda,n, t, i) \\
    \pcind \invecp_{1k}[i] \gets \outvecp_{0i}[k],\ \forall\ k \in \set{n} \\
    \text{\algcom Set party $i$ peer-to-peer messages for all other parties} \\ % Make sure all parties receive peer-to-peer messages
    \adv^{\oraclekg, \oraclefinalize,\RO}(\{ \invecp_{1i} \}_{i \in \corrupt}, \{ \bmsg_i \}_{i \in \honest}, \advstate) \\
    \pcif \roundcounter \neq \numrounds \text{\algcom Prevents trivial win by forcing coordination} \\
    \pcind \pcreturn \randcoin \\
    \pcif \exists i \in \honest : \status_i \neq \accept \\
    \pcind \pcreturn \randcoin \\
    \text{\algcom Honest parties must complete successfully}
    \\
    \pcfor i,j \in \honest \pcdo  \\
    \pcind \pcreturn 1\ \pcif \PK_i \neq \PK_j  \\
    \pcind \pcreturn 1\ \pcif \qual_i \neq \qual_j  \\
    %\text{\ian{Should $\auxinfo$ also be checked here?}}\\
    %CK- we check this in the robustness experiment. I don't the zk and
    %indistinguishability experiemtns should do all the same checks that
    %robustness do, otherwise what is the point of having separate games.
    \SK_0 \gets \dkg[\Hash].\recover(\PK_0, t, \{ (i, \SK_i) \}_{i \in \honest}  ) \\
    \text{\algcom All parties at this point have the same view of $\pk$ and $\qual$} \\
    \pcreturn 1\ \pcif \SK_0 = \fail \\
    \text{\algcom The output DKG keypair given to the distinguisher must be valid} \\
    (\SK_1, \PK_1) \rgets \targetkeygen.\keygen(\lambda) \\
    b' \gets \distinguisher((\PK_b, \SK_b), (\PK_{1-b}, \SK_{1-b})) \\
    \pcreturn 1\ \pcif b' = b \\
    \pcreturn 0
    %
    %
  }
  \end{pcvstack}
  \end{pchstack}

\caption{Indistinguishability experiment for a DKG.
}
\label{fg:indistinguishability}
\end{figure}

