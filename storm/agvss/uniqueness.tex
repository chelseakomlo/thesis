\paragraph{Uniqueness.}
The uniqueness property for an \AVSS is the same as that of the base VSS, as presented in Figure~\ref{fg:vss-unique}.
Notably, the adversary in this experiment can perform aggregation directly,
and so the experiment does not change.

